load("//build:attune.bzl", "attune_population_files")

exports_files([
    "REPORT.md",
    "USAGE.md",
    "evaluation-metrics.parquet",
    "evaluation-proof.parquet",
    "evaluation-regions.parquet",
    "evaluation-telemetry.parquet",
    "replay-proof.parquet",
])

attune_population_files(
    name = "population",
    cases = "population-cases.parquet",
    metadata = "population-metadata.parquet",
    visibility = ["//visibility:public"],
)
